apiVersion: v1
kind: Pod
metadata:
  name: stress-$ID
  annotations:
    simpleddl.scheduling.x-k8s.io/ddl: $DDLs
  labels:
    jobgroup: stress
spec:
  containers:
  - name: stress
    image: progrium/stress:latest
    resources:
      requests:
        cpu: "2000m"
    command:
      - "stress"
      - "-c"
      - "1"
      - "-m"
      - "1"
      - "--vm-bytes"
      - "1000000000"
      - "--timeout"
      - "3600"
